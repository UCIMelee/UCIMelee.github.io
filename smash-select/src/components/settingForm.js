import React, {Component} from "react";
import { Form, FormGroup, ControlLabel, FormControl, Button } from "react-bootstrap";

// passing winner data up, or data to RESET top level state (and anything else)